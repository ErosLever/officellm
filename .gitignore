node_modules/
.pi/
src/office-addin/dist/
bin/
obj/
.specify/
.pi/
node_modules/
dist/
.pi-lens/

# Production config — internal URLs, do not commit
helm/*/values-production.yaml

# Generated auth token — never commit
.env
src/mcp-server/GeneratedToken.cs

# Claude Code project settings contain the token — never commit
.claude/settings.json

# Generated dev manifest with hardcoded localhost URLs
src/office-addin/manifest.dev.xml
